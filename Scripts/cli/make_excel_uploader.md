# make s4 classlists

Make a script like the one below and invoke DuckDB with a command to read it.

> duckdb my_database_db.duckdb -c ".read FILENAME"

``` {.sql} 

.cd '../../../coding/work/work-sql/Scripts/cli/'
.read make_excel_uploader.sql

```
